[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "maras-switchboard"
version = "0.1"
description = "Standalone browser voice runtime for text agents."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "aiohttp>=3.11.0",
  "httpx>=0.28.0",
  "numpy>=1.24.0",
  "rich>=14.0.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0.0",
]
stt-faster-whisper = [
  "faster-whisper>=1.1.0",
]
stt-whisper = [
  "openai-whisper>=20240930",
]
tts-edge = [
  "edge-tts>=6.1.0",
]

[project.scripts]
maras-switchboard = "maras_switchboard.app:main"
maras-switchboard-speak = "maras_switchboard.speak_paragraphs:main"

[tool.hatch.build.targets.wheel]
packages = ["src/maras_switchboard"]

[tool.hatch.build.targets.wheel.force-include]
"src/maras_switchboard/static" = "maras_switchboard/static"
"media/ascii-art.txt" = "maras_switchboard/static/media/ascii-art.txt"

[tool.hatch.build.targets.sdist]
include = [
  "/src/maras_switchboard/**/*.py",
  "/src/maras_switchboard/static/**/*",
  "/media/ascii-art.txt",
  "/tests/**/*",
  "/README.md",
  "/pyproject.toml",
  "/.env.example",
]

[tool.hatch.build.targets.sdist.force-include]
"media/ascii-art.txt" = "media/ascii-art.txt"

[tool.pytest.ini_options]
testpaths = ["tests"]
