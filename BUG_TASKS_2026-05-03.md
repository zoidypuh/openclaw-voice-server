# Bug / UX task list — 2026-05-03

## A. Windows YouTube client volume bug

Path: `/mnt/c/dev/yt-music-client`

Problem:
- The Windows YouTube client still randomly changes volume when the video changes.

Acceptance:
- Video changes must not alter the user's current volume unexpectedly.
- If volume is intentionally restored/synced, it should use the last user-set value only, not a video/default/autoplay value.
- Add logging around video-change and volume-change events while debugging.

## B. Switchboard audio state visibility

Path: `/home/gismar/coding/maras-switchboard`

Problem:
- The display for audio playing/heard is too subtle.

Desired behavior:
- Make state much more visible.
- White when the user is talking / user audio is being heard.
- Orange when Mara/assistant audio is playing.

Acceptance:
- State is obvious at a glance, not tiny/subtle.
- Colors are visually distinct against the dark UI.

## C. Thinking bubbles

Path: `/home/gismar/coding/maras-switchboard`

Problem:
- Existing bubbles are good, but thinking state should have more of them.

Desired behavior:
- When state is `thinking`, show more bubbles / more active bubbling.

Acceptance:
- Thinking state is visibly more animated than idle/listening/speaking.
- Animation should not be distracting when not thinking.

## D. Conversation bubble behavior

Path: `/home/gismar/coding/maras-switchboard`

Problem:
- Conversation section should not scroll the whole page down.

Desired behavior:
- Conversation stays inside a fixed-size rounded bubble/panel.
- As the user speaks, text fills that bubble.
- If text is long, it scrolls inside the bubble; older text disappears/scrolls out within the bubble.
- Same behavior when Mara/assistant speaks.
- Rounded corner / border / accent colors should differ depending on who is speaking.

Acceptance:
- No page-level downward scrolling caused by conversation text.
- Internal overflow/scroll only inside the conversation bubble.
- Speaker state is visually clear via color/accent.

## E. Switchboard Windows client title still has old branding

Path: `/home/gismar/coding/maras-switchboard`

Problem:
- Mara's Switchboard Windows client title still says `OpenClaw Voice...`.

Desired behavior:
- Rename the window/app title to Mara/Switchboard branding.
- Remove old `OpenClaw Voice` title text from visible Windows client UI/title bar.

Acceptance:
- Running Windows client no longer shows `OpenClaw Voice` in the title bar.
- New title uses current Mara/Switchboard branding.
